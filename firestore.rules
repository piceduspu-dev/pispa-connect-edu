rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow everything - no restrictions for activity related operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}